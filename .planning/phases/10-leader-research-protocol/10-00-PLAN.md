---
phase: 10-leader-research-protocol
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/loomkin/teams/agent_research_protocol_test.exs
  - test/loomkin/teams/role_test.exs
autonomous: true
requirements:
  - LEAD-01
  - LEAD-02
must_haves:
  truths:
    - "Test stubs exist for all LEAD-01 and LEAD-02 behaviors"
    - "Stub tests compile and are skipped (not accidentally green)"
    - "role_test.exs has new describe block for research protocol content"
  artifacts:
    - path: "test/loomkin/teams/agent_research_protocol_test.exs"
      provides: "Failing stubs for spawn_type intercept, budget check, awaiting_synthesis transition, and peer_message routing"
    - path: "test/loomkin/teams/role_test.exs"
      provides: "Extended with stub describe block for lead research protocol and researcher findings format"
  key_links:
    - from: "test/loomkin/teams/agent_research_protocol_test.exs"
      to: "lib/loomkin/teams/agent.ex"
      via: "GenServer.start_link + :sys.get_state introspection"
      pattern: "agent_research_protocol_test"
---

<objective>
Create Wave 0 test stub files for all Phase 10 behaviors so Wave 1 plans have failing tests to drive against.

Purpose: Establish the test surface before any implementation — per the established Wave 0 pattern from Phases 7, 8, and 9.
Output: Two test files with skipped stubs that will turn green as Wave 1 and Wave 2 plans implement the feature.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-leader-research-protocol/10-CONTEXT.md
@.planning/phases/10-leader-research-protocol/10-RESEARCH.md

<interfaces>
<!-- Wave 0 stub pattern (reused from Phases 7, 8, 9 exactly): -->
<!-- @moduletag :skip at module level skips all tests in the file -->
<!-- @tag :skip on each test for per-test skip visibility -->
<!-- flunk "not implemented" as placeholder body -->

From test/loomkin/teams/agent_spawn_gate_test.exs (established test helper pattern):
```elixir
defmodule Loomkin.Teams.AgentSpawnGateTest do
  use ExUnit.Case, async: false

  # start_agent helper: starts agent GenServer with test-safe params
  defp start_agent(opts \\ []) do
    # uses Agent.start_link with team_id, name, role, budget assigns
  end
end
```

From test/loomkin/teams/role_test.exs (existing structure to extend):
```elixir
defmodule Loomkin.Teams.RoleTest do
  use ExUnit.Case, async: true
  alias Loomkin.Teams.Role
  # existing describes: "get/1", "tool scoping", "model_for_tier/1"
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent_research_protocol_test.exs stub file</name>
  <files>test/loomkin/teams/agent_research_protocol_test.exs</files>
  <action>
Create `test/loomkin/teams/agent_research_protocol_test.exs` with `@moduletag :skip` at the module level and individual `@tag :skip` on each test. Use `async: false` (GenServer tests require it). Include four describe blocks covering all LEAD-01 behaviors:

1. `describe "spawn_type: :research auto-approve path"` — tests that `run_spawn_gate_intercept` takes the research auto-approve path when `tool_args` contains `"spawn_type" => "research"` or `spawn_type: :research`, bypassing the human gate UI.

2. `describe "budget check still runs for research spawns"` — test that a research spawn exceeding remaining budget returns `{:error, :budget_exceeded, _}` even with `spawn_type: :research`.

3. `describe ":awaiting_synthesis status transition"` — tests that after a research spawn is approved, the agent GenServer transitions to `:awaiting_synthesis` status; and that `handle_cast(:request_pause, %{status: :awaiting_synthesis})` queues the pause rather than immediately pausing.

4. `describe "peer_message routing to tool task during :awaiting_synthesis"` — stub test for the Registry routing: when agent is `:awaiting_synthesis`, incoming peer_message casts are forwarded to the registered tool task pid rather than appended to messages normally.

Each test body: `flunk "not implemented"`. All tests must compile. Use the same `start_agent/1` helper pattern as `agent_spawn_gate_test.exs` — copy the helper signature but leave implementation as a stub too (or import from shared test helper if one exists).
  </action>
  <verify>
    <automated>mix test test/loomkin/teams/agent_research_protocol_test.exs --no-start 2>&1 | grep -E "0 failures|Finished|skipped"</automated>
  </verify>
  <done>File exists, compiles clean, all tests are skipped (0 failures, N skipped shown in output)</done>
</task>

<task type="auto">
  <name>Task 2: Extend role_test.exs with research protocol stubs</name>
  <files>test/loomkin/teams/role_test.exs</files>
  <action>
Append a new `describe "research protocol content"` block to the existing `Loomkin.Teams.RoleTest` module (inside the `defmodule`, after the last existing describe). Use `@tag :skip` on each test (no module-level skip — this is an extension to an existing non-skipped file).

Add two tests:

1. `test "lead role system_prompt contains research protocol section"` — `flunk "not implemented"`. Will eventually assert: `{:ok, %Role{system_prompt: prompt}} = Role.get(:lead)` then `assert prompt =~ "## Research Protocol"` and `assert prompt =~ "team_spawn"`.

2. `test "researcher role system_prompt contains structured findings format"` — `flunk "not implemented"`. Will eventually assert: `{:ok, %Role{system_prompt: prompt}} = Role.get(:researcher)` then `assert prompt =~ "## Research Findings"` and `assert prompt =~ "## Recommendation"`.

Do NOT skip the entire module — existing tests must still run.
  </action>
  <verify>
    <automated>mix test test/loomkin/teams/role_test.exs --no-start 2>&1 | grep -E "failures|skipped"</automated>
  </verify>
  <done>Existing role tests still pass; 2 new tests are skipped (appear in output as skipped, 0 new failures)</done>
</task>

</tasks>

<verification>
Run `mix test test/loomkin/teams/agent_research_protocol_test.exs test/loomkin/teams/role_test.exs --no-start` — existing role tests pass, research protocol stubs are skipped, 0 failures.
</verification>

<success_criteria>
- `agent_research_protocol_test.exs` exists with 4 describe blocks, all skipped
- `role_test.exs` has new research protocol describe block with 2 skipped tests, all other tests still green
- `mix test` full suite passes (skipped tests do not count as failures)
</success_criteria>

<output>
After completion, create `.planning/phases/10-leader-research-protocol/10-00-SUMMARY.md`
</output>
